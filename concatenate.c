#include <stdio.h>

//script that uses printf function to concatenate the string 'sub' and 'rise'

int main (void)
{
char word[] = {'s', 'u', 'n', ' ', 'r', 'i', 's', 'e', '\0'};
printf("%s\n", word);
return 0;
}
